import { TitleDescriptionPipe } from './title-description.pipe';

describe('TitleDescriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new TitleDescriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
