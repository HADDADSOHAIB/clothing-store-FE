import { HeaderFormaterPipe } from './header-formater.pipe';

describe('HeaderFormaterPipe', () => {
  it('create an instance', () => {
    const pipe = new HeaderFormaterPipe();
    expect(pipe).toBeTruthy();
  });
});
